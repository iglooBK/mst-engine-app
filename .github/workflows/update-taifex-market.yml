name: Update TAIFEX + CME Gold + ICE DXY Market Cache

on:
  workflow_dispatch:
  schedule:
    # Taiwan time; multiple post-close attempts handle official publication delay.
    - cron: '20,50 5 * * 1-5'
      timezone: 'Asia/Taipei'
    - cron: '20,50 14 * * 1-5'
      timezone: 'Asia/Taipei'
    - cron: '20 15 * * 1-5'
      timezone: 'Asia/Taipei'

permissions:
  contents: write

concurrency:
  group: taifex-market-cache
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Fetch TAIFEX official market data
        run: python fetch-taifex-market.py
      - name: Fetch CME Gold official settlement data
        id: cme_gold
        continue-on-error: true
        run: python fetch-cme-gold-PRODUCTION.py
      - name: Install ICE DXY parser dependency
        run: python -m pip install --disable-pip-version-check xlrd==2.0.2

      - name: Fetch ICE DXY official close data
        id: ice_dxy
        continue-on-error: true
        run: python fetch-ice-dxy-PRODUCTION.py
      - name: Commit cache only when changed
        shell: bash
        run: |
          if [ -z "$(git status --porcelain -- taifex-market-latest.json cme-gold-latest.json ice-dxy-latest.json)" ]; then
            echo "No TAIFEX / CME Gold / ICE DXY data change."
            exit 0
          fi

          git config user.name "PLOUTOS Market Bot"
          git config user.email "actions@users.noreply.github.com"

          [ -f taifex-market-latest.json ] && git add taifex-market-latest.json
          [ -f cme-gold-latest.json ] && git add cme-gold-latest.json
          [ -f ice-dxy-latest.json ] && git add ice-dxy-latest.json
          if git diff --cached --quiet; then
            echo "No cache file staged."
            exit 0
          fi

          git commit -m "chore: update TAIFEX, CME Gold and ICE DXY market cache"
          git pull --rebase origin "${GITHUB_REF_NAME}"
          git push origin "HEAD:${GITHUB_REF_NAME}"

      - name: Fail if CME Gold fetch failed
        if: steps.cme_gold.outcome == 'failure'
        run: |
          echo "::error::CME Gold official settlement fetch failed."
          exit 1

      - name: Fail if ICE DXY fetch failed
        if: steps.ice_dxy.outcome == 'failure'
        run: |
          echo "::error::ICE DXY official close fetch failed."
          exit 1
